# Progress

- Implemented `moveSurahHeader` in `reflowStore.ts`
- Added drag logic to `Artboard.tsx` handling `surah-open` bounds
- Modified `UnifiedStoryEditor.tsx` to use dynamic SVG overlay dashed boxes instead of a single `outline`
- Written Puppeteer test in `e2e/surahDrag.test.ts`
- Verified build succeeds
- Task completed
